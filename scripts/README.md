# Scripts

The scripts placed in this directory SHOULD NOT be called directly but through
a recipe placed in the `justfile` at root level.

They SHOULD be documented in `docs/contrib/installation-locale.md`.
